/** Corner-radius tokens. Use `rounded-lg`/`rounded-md`/`rounded-sm` Tailwind utilities, which read these CSS variables. */
export const AppRadii = {
  lg: 'var(--radius-lg)',
  md: 'var(--radius-md)',
  sm: 'var(--radius-sm)',
} as const;
